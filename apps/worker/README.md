# Worker Service Placeholder

This workspace is reserved for asynchronous workloads such as:

- feed imports
- media processing
- website publishing jobs
- notifications
- marketplace syncs

The worker should consume contracts and domain packages from `packages/*` instead of forking business logic.
